// TODO: Create a function that returns a license badge based on which license is passed in
// If there is no license, return an empty string
function renderLicenseBadge(name, licenseData) {
  if (name !== 'None') {

  } else {
    return '';
  }
}

// TODO: Create a function that returns the license link
// If there is no license, return an empty string
function renderLicenseLink(name) {

}

// TODO: Create a function that returns the license section of README
// If there is no license, return an empty string
function renderLicenseSection(name) {

}

// TODO: Create a function to generate markdown for README
async function generateMarkdown(readmeContent, licenseKey) {
  // let name = readmeContent.license;
  console.log(licenseKey);
  // let link = await renderLicenseLink(licenseKey);
  //  let badge = renderLicenseBadge(readmeContent.license);
  
  
  //return `# ${readmeContent.title}`;
}

module.exports = generateMarkdown;